#include "main.h"

/**
 *print_rev - reverses string
 *
 *@s: string variable
 *
 *Return: 0
 */

void print_rev(char *s)
{
	int len = 0;

	while (slen] != '\0')
	{
		len++;
	}
	while (len)
	{
		_putchar(s[--len]);
	}
	_putchar('\n');
}
