#include "main.h"
#include <stdio.h>

/**
 * _putchar - check the code.
 *@c: variable
 * Return: Always 0.
 */

int _putchar(char c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
