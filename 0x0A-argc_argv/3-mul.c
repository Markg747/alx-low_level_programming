#include <stdio.h>

/**
 *main - multiplies two numbers
 *
 *@argc: argument count
 *@argv: argument vector
 *
 *Return: 0
 */

int main(int argc, char *argv[])
{
	int i;

	if (argc > 1)
	{
		for (i = 1; i < argc; i++)
		{
			sum = sum + argv[i]
		}
		printf("%d\n", sum);
		if (argv[i] 
	}
	if (argc == 0)
	{
		printf("%d", 0);
	}
